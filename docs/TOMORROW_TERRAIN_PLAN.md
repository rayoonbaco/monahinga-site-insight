# Tomorrow Terrain Plan

Generated by `patch_morning_command_center.py` on 20260513_163133.

## What improved tonight

- The 3D viewer moved to a lighter, faster interactive payload.
- Terrain Texture became the default high-value view.
- The visual tone moved from harsh/dark toward a more scientific balanced relief product.
- The project now has a clearer command flow: protect speed first, then improve source quality.

## Tomorrow's recommended order

1. Run the same BBox twice.
   - First run = full build speed.
   - Second run = cache/repeat-run behavior.
2. Screenshot the same camera angle for:
   - Terrain Texture
   - Hillshade
   - LRM_Edges
   - Discovery
3. Decide the next branch:
   - If speed is still painful: profile PNG export, layer JSON generation, and repeated file writes.
   - If speed is acceptable: improve DEM/source routing metadata and Copernicus readiness.
   - If visuals are the bottleneck: tune Terrain Texture contrast only, not the raw terrain math.
4. Keep archaeology language cautious.
   - Candidate feature, anomaly, signal, field-check target.
   - Never confirmed site without field validation.

## Protected wins

- Keep the fast `heightmap_viewer.json` path.
- Keep the lighter 3D mesh unless visual testing proves a higher mesh is worth it.
- Do not rewrite Hillshade/LRM/Openness/SVF formulas tomorrow morning.
- Do not replace U.S. bare-earth terrain with Copernicus for archaeology detail where USGS/3DEP is better.

## High-value next pass

**Source Quality + Cache Discipline**

Add a source-status panel and cache behavior that explains:

- source used
- source confidence
- DEM resolution class
- whether output came from cache
- whether Copernicus is ready or only a future fallback

This is the cleanest next step because it improves scientific trust without risking tonight's visual and speed gains.
